
on:
  push:
    branches:
      - main
    paths:
      - '**/*.csv'

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Script
        run: |
          echo "A CSV file was pushed to main!"
